The repository provides a Streamlit‑based heart‑disease risk predictor that loads a pre‑trained RandomForest model (heart_disease_model.pkl), uses a cached shap.TreeExplainer for instant feature‑importance visualizations, and includes scripts for training, scaling, and documentation; to run it, install the pinned dependencies from requirements.txt and execute `streamlit run app.py`.